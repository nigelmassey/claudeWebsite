services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - Gallery__Password=${GALLERY_PASSWORD}
      - Gallery__StoragePath=/app/PhotoStorage
      - Api__BaseUrl=http://api:5010/
      - Api__ApiKey=${API_KEY}
    volumes:
      - photo_storage:/app/PhotoStorage
    networks:
      - internal
    depends_on:
      - api

  api:
    build:
      context: .
      dockerfile: claudeWebsite.Api/Dockerfile
    restart: unless-stopped
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - Gallery__StoragePath=/app/PhotoStorage
      - ApiKey=${API_KEY}
    volumes:
      - photo_storage:/app/PhotoStorage
    networks:
      - internal

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/claudewebsite.conf:/etc/nginx/conf.d/default.conf:ro
      - certbot_certs:/etc/letsencrypt:ro
      - certbot_www:/var/www/certbot:ro
    networks:
      - internal
    depends_on:
      - app

  certbot:
    image: certbot/certbot
    volumes:
      - certbot_certs:/etc/letsencrypt
      - certbot_www:/var/www/certbot
    # Not started automatically — run manually (see deployment notes)

volumes:
  photo_storage:
  certbot_certs:
  certbot_www:

networks:
  internal:
    driver: bridge
    ipam:
      config:
        - subnet: 172.28.0.0/16
